<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Candidates - Voting App</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-100">

  <header class="bg-white shadow-sm sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center py-4">
        <a href="/" class="flex items-center space-x-2"><svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-indigo-600" viewBox="0 0 20 20" fill="currentColor"><path d="M4 3a2 2 0 100 4h12a2 2 0 100-4H4z" /><path fill-rule="evenodd" d="M3 8h14v7a2 2 0 01-2 2H5a2 2 0 01-2-2V8zm5 3a1 1 0 011-1h2a1 1 0 110 2H9a1 1 0 01-1-1z" clip-rule="evenodd" /></svg><h2 class="text-2xl font-bold text-slate-900">Voting App</h2></a>
        <nav class="hidden md:flex items-center space-x-8 text-base font-medium">
          <a href="/" class="text-slate-600 hover:text-indigo-600">Home</a>
          <a href="/candidate" class="text-indigo-600">Candidates</a>
          <a href="/candidate/results" class="text-slate-600 hover:text-indigo-600">Results</a>
        </nav>
      </div>
    </div>
  </header>

  <main class="py-12 md:py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h1 class="text-4xl font-extrabold text-slate-900 tracking-tight sm:text-5xl">Meet the Candidates</h1>
        <p class="mt-4 max-w-2xl mx-auto text-xl text-slate-600">Review the candidates below and cast your vote to make a difference.</p>
      </div>
      
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
        <% if(candidates && candidates.length > 0) { %>
          <% candidates.forEach(candidate => { %>
            <div class="bg-white rounded-xl shadow-lg overflow-hidden flex flex-col justify-between">
              <div class="p-6">
                <div class="flex items-center space-x-4">
                  <img class="h-16 w-16 rounded-full object-cover" src="https://ui-avatars.com/api/?name=<%= candidate.name.replace(' ', '+') %>&background=random" alt="<%= candidate.name %>">
                  <div><h3 class="text-xl font-bold text-slate-900"><%= candidate.name %></h3><p class="text-indigo-600 font-semibold"><%= candidate.party %></p></div>
                </div>
                <div class="mt-6 flex justify-between items-center text-sm text-slate-600"><p>Age: <%= candidate.age %></p><p class="font-bold text-lg text-slate-800">Votes: <%= candidate.voteCount %></p></div>
              </div>
              
              <!-- ============================================= -->
              <!-- CORRECTED DYNAMIC ACTION AREA -->
              <!-- ============================================= -->
              <div class="p-4 bg-gray-50 border-t">
                <% if (user && user.role === 'admin') { %>
                  <!-- ADMIN VIEW: Show Edit and Delete buttons -->
                  <div class="flex items-center space-x-2">
                      <a href="/candidate/edit/<%= candidate._id %>" class="flex-1 text-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50">Edit</a>
                      <form action="/candidate/delete/<%= candidate._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this candidate?')" class="flex-1">
                          <button type="submit" class="w-full text-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700">Delete</button>
                      </form>
                  </div>
                <% } else if (user && user.role === 'voter') { %>
                  <!-- VOTER VIEW: Check if they have voted -->
                  <% if (user.isVoted) { %>
                    <button disabled class="w-full flex items-center justify-center px-4 py-2 border border-transparent text-base font-medium rounded-md text-green-700 bg-green-100 cursor-not-allowed">
                      <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                      Voted
                    </button>
                  <% } else { %>
                    <form action="/candidate/vote/<%= candidate._id %>" method="POST">
                      <button type="submit" class="w-full text-center px-4 py-2 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700">Vote Now</button>
                    </form>
                  <% } %>
                <% } else { %>
                  <!-- GUEST VIEW: Not logged in -->
                  <a href="/login" class="w-full block text-center px-4 py-2 border border-transparent text-base font-medium rounded-md text-indigo-700 bg-indigo-100 hover:bg-indigo-200">
                    Login to Vote
                  </a>
                <% } %>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <p class="col-span-full text-center text-slate-500">No candidates have been registered yet.</p>
        <% } %>
      </div>

      <% if (user && user.role === 'admin') { %>
        <div class="mt-16 text-center">
            <a href="/candidate/add" class="inline-flex items-center px-6 py-3 border border-transparent shadow-sm text-base font-medium rounded-md text-white bg-green-600 hover:bg-green-700">Add New Candidate</a>
        </div>
      <% } %>
    </div>
  </main>
</body>
</html>